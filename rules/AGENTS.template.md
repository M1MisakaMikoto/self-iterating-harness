# AGENTS.md（模板）

> 复制本文件到项目根并命名为 `AGENTS.md`，将 `<...>` 占位替换为具体内容。
> 框架要求：保持"工作步骤 + 工作规范"两段式结构，编号自增。

必须遵循以下工作步骤：

1. <步骤一：例如"分析需求时确认细节，无因果关系的问题批量提问">
2. <步骤二：例如"编写方案（对齐后的原始需求 + 改动前后对比），用户确认后实施">
3. <步骤三：例如"实施后评估结果与方案是否一致">

必须遵守以下工作规范：

1. <规范一：例如"描述性任务用 mermaid 逻辑图/类图，禁止浅色背景">
2. <规范二>
3. <规范三>
